from riot_client.window import window, webview
from riot_client.main import start_app
from riot_client.lcu_connector import start_connector
def start(window):
    print("starting")

if __name__ == '__main__':
    start_app()
    ## webview.start(start,(window,),debug=True)

    
